import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ml.mdriaz.bmiCalc',
  appName: 'BMI Calculator',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
